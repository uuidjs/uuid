{
  "name": "uuid-example-browser-umd",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "true",
    "start": "npm run build && npx http-server . -o"
  },
  "dependencies": {
    "uuid": "file:../../.local/uuid"
  }
}
